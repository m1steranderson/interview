# Cloudflare KV (REST API)
CF_ACCOUNT_ID=your_account_id
CF_API_TOKEN=your_api_token
CF_NAMESPACE_ID=your_kv_namespace_id

# NATS
NATS_URL=nats://localhost:4222

# HMAC secret (shared with web & kv-service workers)
KV_SECRET=change-me-in-production
WEB_APP_URL=http://localhost:3000

# KV Service URL (for read-back verification)
KV_SERVICE_URL=http://localhost:8786

# HTTP timeout for outbound requests (ms)
HTTP_TIMEOUT_MS=5000
